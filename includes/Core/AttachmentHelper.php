<?php
declare(strict_types=1);

namespace ContactInbox\Core;

/**
 * Attachment Helper - Stub for Free Version
 * 
 * File attachments are a Pro-only feature.
 * This stub prevents fatal errors where the class is referenced.
 */
final class AttachmentHelper {

    /**
     * Extract file paths from attachment data (stub)
     * 
     * @param mixed $attachment Attachment data
     * @return array Empty array (attachments are Pro-only)
     */
    public static function extract_file_paths($attachment): array {
        // Pro feature - return empty array
        return [];
    }
}
